package org.black_matter.monospace.input;

public class Input {
	
	public void onKey(int key, KeyAction action) {
		/*if(Raylib.IsKeyPressed(key)) action.onPress();
		if(Raylib.IsKeyDown(key)) action.onDown();
		if(Raylib.IsKeyReleased(key)) action.onRelease();*/
	}
}
